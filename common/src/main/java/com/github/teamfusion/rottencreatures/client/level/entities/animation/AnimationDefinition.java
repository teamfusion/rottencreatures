package com.github.teamfusion.rottencreatures.client.level.entities.animation;

public class AnimationDefinition {
    protected static float toRad(float degrees) {
        return (float) Math.toRadians(degrees);
    }

    protected static float toRad(double degrees) {
        return (float) Math.toRadians(degrees);
    }

    protected static float toRad(int value) {
        return (float) Math.toRadians(value);
    }
}